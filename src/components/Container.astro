---
interface Props {
  class?: string;
  wide?: boolean;
}
const { class: className = '', wide = false } = Astro.props;
const maxW = wide ? 'max-w-7xl' : 'max-w-6xl';
---

<div class={`mx-auto w-full ${maxW} px-6 lg:px-8 ${className}`}>
  <slot />
</div>
